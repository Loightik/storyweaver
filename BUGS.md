# Bugs - StoryWeaver

Пиши баги сюда (по одному в строке под Open), потом:
`java -jar app/target/app-1.0.0-SNAPSHOT.jar fix --plugin storyweaver`

Или сразу:
`java -jar app/target/app-1.0.0-SNAPSHOT.jar fix --plugin storyweaver --bug "описание"`

## Open
-

## Fixed
-
